{% extends 'base_user.html.twig' %}

{% block title %}Détails lieu {% endblock %}



{% block body %}
    <div class="container-fluid">
        <div  style = "margin-left: auto ; margin-right: auto ; max-width : 900px;">
            <p class="display-6 no-wrap text-center align-center mb-5"> Détails du lieu {{location.getName()}}</p>
            
            <div class=" d-flex text-center justify-content-evenly">
                <div class="card justify-content-start" style="width: 18rem;">
            
                    <div class=" card-body ">
                        <h5 class="card-title">{{location.getName()}}</h5>
                    </div>
                    <ul class="list-group list-group-flush">                       
                        <li class="list-group-item"> Nom : {{location.getName()}}</li>
                        <li class="list-group-item"> Adresse: {{location.getAdress()}}</li>
                        <li class="list-group-item">ZIP code :   {{location.getName()}}</a></li>
                        <li class="list-group-item">Ville :  {{location.getCity()}} </li>                   
                        <li class="list-group-item">Fournisseurs liés :{% for supplier in location.getSuppliers() %}<a href="/supplier/show/{{ supplier.getId()}}" class="card-link">  {{supplier.getName()}}</a> {% endfor %} </li>                   
                        <li class="list-group-item">Clients liés : {% for customer in location.getCustomers() %}<a href="/customer/show/{{ customer.getId() }}" class="card-link">  {{customer.getName()}} {% endfor %}</a></li>                   
                    </ul>
                    <div class="card-body">             
                            <a href="/location/edit/{{ location.getId() }}" class="card-link"><i class="fa-solid fa-pen-to-square"></i></a>       
                          
                    </div>
                </div>
            </div>
        </div> 
       
        
            </table>
        </div>

       
    </div>
{% endblock %}
